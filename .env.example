# ── API Keys (cloud providers) ────────────────────────────────────────────
ANTHROPIC_API_KEY=
OPENAI_API_KEY=
GEMINI_API_KEY=

# ── LiteLLM ───────────────────────────────────────────────────────────────
# Master key para acceder a la API de LiteLLM (cámbiala)
LITELLM_MASTER_KEY=sk-localdev-changeme

# Salt para encriptar API keys guardadas en BD (genera uno con: openssl rand -hex 32)
LITELLM_SALT_KEY=

# ── Postgres + Grafana admin (misma contraseña para simplificar) ──────────
POSTGRES_PASSWORD=changeme
